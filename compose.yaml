name: bank-front-lab

services:
  bank-front:
    build:
      context: .
      dockerfile: Dockerfile

    restart: unless-stopped

    ports:
      - "127.0.0.1:8086:8080"

    networks:
      - bank-net

networks:
  bank-net:
    external: true
    name: bank-net
